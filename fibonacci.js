/* 
Faz a equação de fibonacci n vezes
A cada vez, atualiza o valor e insere no array
*/

function fibonacci(n) {
  if (typeof n !== "number") {
    return
  }

  const fibs = [];

  while (x <= n) {
    x++
  }
}